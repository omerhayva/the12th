import Link from 'next/link';
import { matches } from '@/lib/demo-data';

export default async function MatchPage({ params }: { params: Promise<{ id: string }> }) {
  const { id } = await params;
  const match = matches.find((m) => m.id === id) ?? matches[0];
  return <main className="page"><div className="shell"><nav className="nav"><Link href="/" className="brand"><div className="mark">12</div><span>THE 12TH</span></Link><div className="navRight"><Link href="/matches">MAÇLAR</Link><Link href="/leaderboard">LEADERBOARD</Link><Link href="/profile">MY IQ</Link></div></nav><section className="hero"><div><div className="eyebrow">LIVE MATCH / {match.minute}&apos;</div><h1>{match.home.toUpperCase()}<br/><span style={{color:'var(--green)'}}>VS {match.away.toUpperCase()}</span></h1><p className="sub">Skor {match.homeScore} — {match.awayScore}. Maçın kritik anında sıra sende.</p></div></section><div className="grid"><div className="card"><div className="cardHead"><span className="label">THE 12TH DECISION</span><span className="label">10 SEC</span></div><h2 style={{fontSize:28,margin:'20px 0'}}>Rakip blok geride. Takımın şimdi nasıl oynamalı?</h2><div className="options"><button className="option"><small>01</small><strong>BASKI</strong><div>Rakibi kendi yarı alanına it</div></button><button className="option"><small>02</small><strong>GERİ ÇEKİL</strong><div>Blokları daralt, kontrayı bekle</div></button><button className="option"><small>03</small><strong>DEĞİŞİKLİK</strong><div>Oyuncu değişikliğiyle dengeyi değiştir</div></button></div><button className="submit">KARARINI VER →</button></div><aside className="side"><div className="card"><span className="label">FAN PULSE</span><div className="pulse"><div className="bar"><i/><i/></div><div className="split"><span>{match.homeShort} 61%</span><span>{match.awayShort} 39%</span></div></div></div><div className="card"><span className="label">MATCH TIMELINE</span><div className="timeline"><div className="event"><span className="time">{match.minute}&apos;</span><div><b>12TH DECISION</b><p>Karar bekleniyor.</p></div></div><div className="event"><span className="time">57&apos;</span><div><b>ŞUT</b><p>Ceza sahası dışından deneme.</p></div></div><div className="event"><span className="time">51&apos;</span><div><b>GOL</b><p>Hızlı hücum sonrası skor değişti.</p></div></div></div></div></aside></div></div></main>;
}
